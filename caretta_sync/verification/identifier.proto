syntax = "proto3";
package caretta_sync.verification;

import "caretta_sync/common/uuid.proto";
import "mtid/dtid.proto";

message Identifier {
    oneof value {
        mtid.Dtid device_id = 1;
        mtid.Dtid verification_id = 2;
    }
}
