syntax = "proto3";

package caretta_sync.api.device;

import "caretta_sync/api/invitation_token/invitation_token.proto";

message InviteResponse {
    caretta_sync.api.invitation_token.InvitationToken token = 1;    
}
