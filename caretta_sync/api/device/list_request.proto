syntax = "proto3";

package caretta_sync.api.device;

message ListRequest{
    enum Status {
        UNSPECIFIED = 0;
        DISCOVERED = 1;
        VERIFIED = 2;
        PENDING_VERIFICATION = 3;
    }
    repeated Status statuses = 1;
}