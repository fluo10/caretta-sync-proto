syntax = "proto3";

package caretta_sync.api.invitation_token;

message ListRequest{
    enum Status {
        UNSPECIFIED = 0;
        Pending = 1;
        Used = 2;
        Revoked = 3;
        Expired = 4;
    }
    repeated Status statuses = 1;
}