syntax = "proto3";

package caretta_sync.api.device_verification;

import "mtid/dtid.proto";
import "caretta_sync/api/device/identifier.proto";


message Identifier {
    oneof value {
        mtid.Dtid verification_id = 1;
        caretta_sync.api.device.Identifier device = 2;
    }
}