syntax = "proto3";
package caretta_sync.api.device_verification;
import "caretta_sync/types/iroh/doc_namespace_secret.proto";

message ConfirmResponse {
    // Doc Secret of verified device document
    caretta_sync.types.iroh.DocNamespaceSecret doc_secret = 1;
}